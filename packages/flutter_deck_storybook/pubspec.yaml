name: flutter_deck_storybook
description: Reusable storybook pages and page-turn transitions for flutter_deck.
publish_to: none
resolution: workspace

version: 0.1.0

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  flutter_deck: ^0.29.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
