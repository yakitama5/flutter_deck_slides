# リスくんと ひとつのどんぐり

Issue #28で作成した絵本候補から選定した画像を、通常の説明スライドとつないだ
FlutterDeckです。前半で自己紹介・会社紹介を行い、絵本をはさんで後半に実体験の
紹介へ移る構成になっています。

読み聞かせ用のスピーカーノートを設定しています。原稿は `lib/speaker_notes.dart` に
まとめているので、読み聞かせる文章だけを変更する場合は、このファイルを編集してください。

会社名・実績・体験談はサンプル文言なので、発表前に `lib/main.dart` の該当スライドを
差し替えてください。

## スライド構成

- 01: タイトル
- 02: 自己紹介
- 03: 会社紹介（サンプル）
- 04: 絵本へのブリッジ
- 05: 絵本の前カバー
- 06–17: 絵本本文 12ページ
- 18: 絵本の裏表紙
- 19: 実体験の紹介
- 20: 実体験の流れ（サンプル）
- 21: 持ち帰り

通常スライドは Material 3 のカード・チップを使った軽いフェードで切り替わり、
前カバーから本文へ入る時は本を開き、本文から裏表紙へ出る時は本を閉じます。
本文中は従来どおりページめくりと線画からのインク・水彩リビールが動きます。

## 絵本本文の採用画像

- 外側の表紙: `StorybookBookCover` + タイトル
- 01: 旧表紙絵 `cover_master.png`（カバーをめくった後のタイトルページ）
- 02: ロック済み `page01_master.png`
- 03: `page02` の `variant_c`
- 04: `page03` の `variant_b`
- 05: `page04` の `variant_c`
- 06: `page05` の `variant_a`
- 07: `page06` の `variant_a`
- 08: `page07` の `variant_b`
- 09: `page08` の `variant_a`
- 10: `page09` の `variant_b`
- 11: `page10` の `variant_a`
- 12: `page11` の `variant_c` + 「おしまい」リボン

採用画像は `assets/risukun_hitotsu_no_donguri/` にコピーしています。リボン付きの最終ページは
`11_page11_ending.png` として元画像を残したまま追加しています。
候補画像・コンタクトシートの原本は、
`slides/2026_oso_risukun_hitotsu_no_donguri/generation/output/` に残しています。

## 起動

```sh
dart run melos run dev
```

起動後、対象パッケージとして `slides/20260912_oso` を選択します。
